# Dogfood — `LeeHyeonKyu/know-thy-build-demo` (Plan 6)

사람이 읽는 관찰 기록이다(retro가 아니다). 실행마다 한 줄: 무엇을 했고, 무엇이 나왔고, 얼마나 걸렸고, 얼마를 썼고, 어떤 결함이 나왔는지.

- **대상**: `LeeHyeonKyu/know-thy-build-demo` (private, 일회용). 스파이크 산출물은 태그 `spikes-done`이 보존.
- **KTB**: 브랜치 `spec/factory-1.0`. 결함 수정은 전부 여기에 커밋한다.
- **환경**: macOS 15(Darwin 24.6), Node 24.14(로컬) / 저장소 선언은 Node 22, Docker Desktop(compose v5.1.1).

---

## Task 1 — 데모 repo 준비(픽스처 + 정리), 로컬 전용

토큰 소비 **0** (`claude -p`·`factory run`·`factory bootstrap` 미실행).

| # | 단계 | 명령 | 결과 | 소요 | 비용 | 결함 |
|---|---|---|---|---|---|---|
| 1 | 스파이크 잔재 제거 | `git rm -r .claude .github/actions .github/workflows/spike-9-agent-type.yml spikes` | 18 파일 삭제, `spikes-done` 태그가 보존 | <1s | 0 | — |
| 2 | 픽스처 작성 | (수기) `docs/PROJECT.md` · `docs/TECHNICAL.md` · `docs/QA.md` · `docs/features/001~003` · `docs/factory/CHARTER.md`(`status: ready`) · `.factory/harness.toml`(M1) · `CLAUDE.md` | P6-R1대로 Phase 1 산출물을 손으로 작성 | — | 0 | — |
| 3 | init | `node <ktb>/bin/cli.js factory init` | created 133 · replaced 0 · merged 0(settings 병합 대상 없음 — 스파이크 settings.json은 2단계에서 삭제됨) · skipped 0 | <2s | 0 | — |
| 4 | init 재실행(멱등성) | 같은 명령 | created 0 · skipped 133 — 덮어쓰기 없음 확인 | <2s | 0 | — |
| 5 | doctor #1 | `factory doctor` (전체) | **중단** — `docker compose up -d --wait`가 `postgres:16-alpine` pull에서 무한 대기 | 16m 후 kill | 0 | **E1**(환경) |
| 6 | 컴포즈 이미지 교체 | `docker-compose.test.yml`: `postgres:16-alpine` → `postgres:16` | 로컬에 이미 있는 이미지로 전환 → compose healthy 3s | 5s | 0 | E1 우회 |
| 7 | doctor #2 | `factory doctor` (전체) | PASS 57 · WARN 3 · FAIL 2 — `commands.run.unit` FAIL(`service "db" is not running`) + github 1 FAIL/3 WARN | ~2m | 0 | **D1** |
| 8 | D1 수정(KTB, TDD) | `npx vitest run` (1178 tests) | RED → GREEN, 전체 스위트 통과 | 2m 36s | 0 | D1 |
| 9 | upgrade | `factory init --upgrade` | created 0 · replaced 0 · kept 18 · pruned 0 (D1은 `factory/cli/`라 설치 대상 아님 — 재설치 불필요 확인) | <2s | 0 | — |
| 10 | doctor #3 (최종) | `factory doctor` (전체) | **PASS 58 · WARN 3 · FAIL 1** — FAIL·WARN 전부 `github.*`(bootstrap 미실행) | 13s | 0 | — |
| 11 | 커밋 + push | `git commit` → `git push origin main` | main 갱신 | <5s | 0 | — |

**최종 doctor 요약**: `doctor: PASS 58 · WARN 3 · FAIL 1`
- FAIL: `github.bot-token`(Task 2의 H1 체크포인트에서 등록)
- WARN: `github.labels` · `github.protection` · `github.token-issued-at` (전부 `factory bootstrap` 전이라 정상)
- **non-github FAIL 0** — Task 1의 종료 조건 충족.

### KTB 결함 목록

| id | sha | 한 줄 |
|---|---|---|
| D1 | `6e4af3f` | `factory doctor`가 `[commands]`를 하네스 테스트 환경 **밖에서** 실행해, `[test.env].compose`가 있는 M1+ 저장소는 `commands.run.unit`이 상시 FAIL이었다. CI(`.factory/actions/setup`, `test-env: true`)와 같은 순서가 되도록 envUp/envDown이 명령 게이트와 스모크를 한 사이클로 감싸게 고쳤다(테스트 추가: `doctor-cli.test.js`의 env-wraps-commands). |
| KTB-2 | `ca65348` | `factory bootstrap`을 GitHub Free 플랜의 private repo에 돌리면 branch protection PUT이 403(`Upgrade to GitHub Pro or make this repository public…`)로 실패했는데, 실패 한 줄이 두 번(`applyBootstrap`의 `log()` + `bootstrapCommand`의 요약 루프) 찍히고 종료 코드가 뭘 뜻하는지 불명확했다. `bootstrap.js`(cli+lib)는 이 403을 한 줄만 찍도록 dedupe하고(`protection main: not available on this plan (private repo on GitHub Free) — make the repo public or upgrade; L0 required-check enforcement is off, L1 (merge script requires all checks GREEN) and L2 still apply`), exit 1을 명확히 하면서도 다른 op은 전부 적용을 유지한다. `doctor`의 `github.protection`도 같은 403을 FAIL이 아니라 WARN(플랜 안내)으로 구분한다(404는 그대로). 테스트: `bootstrap.test.js`(fix round 2)·`gh.test.js`·`doctor-factory.test.js`(fix round 2). |
| KTB-3 | `ca65348` | `factory doctor`가 env-up 실패 시 스모크는 건너뛰면서(`envResult.ok` 게이트) `[commands]`는 그대로 실행하는 비대칭이 있었다 — DB가 필요한 M1+ 저장소는 env-up이 죽어도 `commands.run.*`가 환경 없이 돌아 고칠 수 없는 거짓 결과를 낸다. `checkCommands`에 `skipReason`을 추가해 env-up 실패 시 각 `commands.run.<k>`를 실행 없이 `FAIL: skipped: test env not up (<reason>)`로 보고하도록 smoke와 대칭을 맞췄다(`envDown`은 그대로 항상 호출). 테스트: `doctor-cli.test.js`(Task 3). |
| KTB-4 | `09fb3e6` | `factory/cli/doctor.js`가 gh client를 `makeGh({ run, repo: process.env.FACTORY_REPO || "" })`로 만들었다 — 사람이 `FACTORY_REPO` 없이(정상 케이스) `factory doctor`를 돌리면 `repo`가 `""`가 돼 `gh api repos//branches/main/protection`이 실패하고, 그 non-403 실패가 `null`로 떨어져 실제로는 존재하는 branch protection(`contexts=["factory/integrity"]`, real repo로 확인)을 `github.protection — branch protection is missing L0 contexts: factory/integrity`로 오보했다. `status.js`가 이미 하던 해석(`FACTORY_REPO` → `gh repo view --json nameWithOwner`)을 `resolveRepo({ run })`로 `factory/lib/gh.js`에 공유 추출해 `doctor.js`·`status.js` 둘 다 쓰게 고쳤다. 해석 자체가 실패하면(로그인 안 됨·git repo 아님) `github.*` 전체를 건너뛰고 `github.unavailable` WARN 하나로 남긴다(오프라인 허용, 오보 아님). `bootstrap.js`/`run.js`/`factory/bin/*.js`도 같은 `|| ""` 패턴을 찾아봤으나 전부 이미 `FACTORY_REPO || gh repo view` 방식을 인라인으로 쓰고 있어 결함은 없었다(추가 수정 없음). 테스트: `gh.test.js`(resolveRepo 유닛 3건)·`doctor-cli.test.js`(KTB-4 2건: 해석된 repo로 protection 호출, 해석 실패 시 github.unavailable). |
| KTB-5 | this commit | **integrity(L0)가 사람의 머지까지 막고 있었다.** `factory/lib/integrity.js`는 `[protected].factory` 매치 파일(`.factory/**`·`.claude/**`·`docs/factory/CHARTER.md`·`package.json` …)이 diff에 있으면 `protected path changed` 위반으로 exit 1 했는데, `factory/integrity`는 branch protection에 걸린 **유일한** required context다(ADR-015 보강, `enforce_admins: true`). 그래서 빌드 설정을 건드리는 PR은 **누구도** 머지할 수 없었다 — 설계가 전제하는 사람 머지 경로(retro-proposal PR, `factory:harness` 승격 PR, 사람-지점 스킬이 만드는 PR)와 데모 PR #1(`chore(factory): init --upgrade`, 검증 시점 RED) 전부가 여기 걸린다. 스펙의 의도(§5.1·ADR-015)는 "봇의 **자동** 머지를 막고 사람이 머지한다"였다. 두 관심사를 갈랐다(ADR-020 KTB-5): **L0**(`integrity` 체크)는 **변조만** 본다 — additive-only 위반, 위조 헤더, lessons 포맷, 테스트 skip/ignore pragma, 판정 불가. 보호 경로 변경은 결과의 `protected` 목록과 잡 로그 한 줄(`integrity: protected paths changed (human merge required): …`)로만 남고 체크를 RED로 만들지 않는다. **L1**(merge 스테이지)이 **게이트를 돌리기 전에** `protectedPaths()`로 PR 범위를 세어, 하나라도 있으면 머지하지 않고 `factory:needs-human`으로 전이한다(이슈에 사유 한 줄 + PR에 파일 나열 코멘트, exit 2 — 계산 실패·dep 부재는 `factory:blocked`, fail closed). **fix round 1**에서 세 구멍을 더 막았다: ① rename/copy 줄의 **출발지**를 세지 않아 `factory-integrity.yml` → `ci-integrity.yml` 같은 이동이 검사를 통째로 빠져나갔다(`--no-renames` + 모든 경로 필드 파싱, 그리고 훅이 보호 경로의 `git rm`/`git mv`를 차단), ② 보호 경로 검사가 `gates` **뒤**에 있어 거부될 PR의 코드가 머지 잡 안에서 먼저 실행됐다(순서를 앞으로), ③ 게이트 하위 프로세스가 머지 권한 토큰을 물려받았다(`scrubEnv`/`scrubbedRunner`로 6개 자격증명 제거). 이 판정은 **base 브랜치의 코드·하네스**로 한다(모듈과 `harness`는 `checkoutHead` 이전에 로드된다; PR의 `.factory/bin/integrity.js`를 하위 프로세스로 부르지 않고 파일 내용도 읽지 않는다 — name-status diff만 본다). retro의 다크 lessons PR에는 `integrity.protected`가 비어 있지 않으면 push조차 하지 않는 assert를 더했다. 테스트: `integrity.test.js`(KTB-5 7건)·`merge-stage.test.js`(4b 6건)·`run-stage.test.js`(호출 순서 + 거부 1건)·`retro-publish.test.js`(다크 PR assert). |
| KTB-6 | this commit | **같은 가족의 두 번째 구멍 — `additive_only`도 L0 변조 규칙이었다.** KTB-5 수정을 실어 나르는 평범한 `factory init --upgrade` PR(데모 #3)이 다시 `factory/integrity` RED로 떨어졌다(run `34692991112`): `violations`는 정확히 한 줄, `.claude/agents/factory-builder.md: additive-only sections (## Examples, ## Perspectives) — removals or edits outside allowed sections`. KTB-5의 프롬프트 드리프트 수정이 `## You must not`을 고쳤고 그것이 허용 섹션 밖이었다. 범위는 KTB-5보다 넓다 — 이 규칙은 `/know-thy-build:role`이 여는 **모든** PR과 `.claude/agents/**`를 건드리는 모든 패키지 업그레이드를 사람도 머지할 수 없게 만들고 있었다. 같은 판결을 적용했다(ADR-020 KTB-6): additive-only 발견은 `violations`가 아니라 새 `policy` 배열로 보고되고(L0는 exit 0 + 알림 한 줄), 집행은 L1이 한다 — 새 `policyViolations()`가 merge 스테이지 단계 (3)에서 `protectedPaths()` 바로 뒤·게이트 앞에 돌아 `factory:needs-human`으로 넘긴다. 섹션 판정에는 파일 내용이 필요한데 merge는 PR head를 체크아웃한 트리 위에서 돌므로, 내용은 **워킹 트리가 아니라** `git show <head>:<file>`로만 읽는다(PR이 판정 재료를 고르지 못하게). 판정 본체 `additiveOnlyViolations`는 L0·L1이 공유한다. retro의 다크 PR 선검사도 `policy.length > 0`이면 push하지 않는다(그 PR이 자동 머지되는 근거가 바로 이 규칙이다). L0에 남은 변조 규칙은 셋 — lessons 포맷, 테스트 skip/ignore pragma, 판정 불가. 테스트: `integrity.test.js`(KTB-6 8건)·`merge-stage.test.js`(3b 6건)·`run-stage.test.js`(순서 + 거부)·`retro-publish.test.js`(다크 PR policy assert). |

### 환경 결함(결함 목록 아님 — P6-R5)

| id | 내용 | 대응 |
|---|---|---|
| E1 | 로컬 Docker 데몬이 **새 이미지를 pull 하지 못한다**(`postgres:16-alpine` pull·`docker pull`·`docker run`(미보유 이미지) 모두 무응답, 60s+ 무진행. 이미 보유한 이미지의 `docker run`·compose는 정상). 레지스트리 자체는 도달 가능(`registry-1.docker.io` 401 0.6s). | 데모의 `docker-compose.test.yml`을 로컬 보유 이미지 `postgres:16`으로 바꿔 우회. Docker Desktop은 건드리지 않았다(사용자의 다른 컨테이너 14개 가동 중). CI(ubuntu-latest)에는 영향 없음. |

### 픽스처 판단 기록

| 항목 | 선택 | 이유 |
|---|---|---|
| 저장소(TECHNICAL TDR-1) | **PostgreSQL 16** (SQLite 아님) | 브리프는 "`src/app.js`가 이미 하는 것"을 고르라 했으나 `src/app.js`는 `/healthz`만 있고 DB를 쓰지 않는다. 반면 repo에는 `docker-compose.test.yml`(postgres)과 `test/integration/db.test.js`가 실재하고, **그 파일들이 M1 선언의 근거**다. SQLite로 적으면 선언(TECHNICAL)과 하네스(harness.toml `[test.env]`)가 어긋난다. |
| 스모크 세트 | `unit` + `integration` (e2e 제외) | `docker info` 성공 → 컴포즈 기동 가능. e2e는 존재하지만 M1이므로 게이트 밖(M2 승격은 `factory:harness` 이슈로 관찰). |
| `lint` / `lint_file` | `node -e 0` / `node -e 0 {file}` | 데모에 린터가 없다(컨트롤러 판결). `{file}`은 doctor의 `commands.placeholders`가 요구해서 붙였다 — 무시되는 argv다. |
| `[gates]` 레벨 | 템플릿 그대로(`lint`,`unit` × fast/full/deep) | M1에서 `gates.levels-vs-maturity`가 요구하는 것은 "maturity 상한을 넘는 레벨에 상한 밖 게이트를 두지 말 것"뿐이고 템플릿이 이미 만족한다. |
| `[protected].factory` | 템플릿 + `docker-compose.test.yml` 추가 | 테스트 환경 정의도 판정 기준의 일부다(에이전트가 DB를 바꿔 통과시키는 길을 막는다). |

### 남은 관찰(결함 아님, Task 2 이후에 볼 것)

- M1인데 `[gates].full`에 `integration`이 없다 — 통합 테스트는 `unit` 명령(`npx vitest run`, 전체 실행)에 포함돼 돌지만, 게이트 이름으로 분리돼 있지 않아 `factory.gates.v1` 기록에서 통합 실패가 `unit` RED로 보인다. 승격(M2) 때 `[commands].integration` 분리를 함께 볼 것.
- `envUp`의 `docker compose up -d --wait`에는 타임아웃이 없다 — E1 같은 데몬 지연에서 doctor가 무한 대기한다. CI에서는 job timeout이 잡지만 로컬에서는 사람이 죽여야 한다. 재발하면 결함으로 올린다.
- `skills.installed — not installed` (PASS-info): 스킬 설치는 Task 1 범위 밖(사람이 수동 시연).

## Task 3 — 이슈 1건 다크 완주 시도 (H2, 데모 #2)

`docs/features/001-create-note.md`로 이슈 **#2**를 만들고(`gh issue create --label backlog`)
`transition.js 2 factory:queue --human`으로 큐에 넣었다(2026-09-12T11:30:15Z).
**완주하지 못했다** — triage는 통과, plan에서 `factory:needs-human`. 소각 **$12.12 / 2 runs / 38.5분**.

| 스테이지 | run id | 시작(UTC) | 소요 | 결과 | 라운드 | 라벨 전이 | handoff | 비용 |
|---|---|---|---|---|---|---|---|---|
| (사람) queue | — | 11:30:15 | — | ok | — | `backlog → factory:queue` (human) | transition 코멘트 | $0 |
| triage | 34691217112 | 11:30:22 | 59 s | ✅ success | — | `factory:queue → factory:ready` (script) | ✅ 있음 (tier **standard**) | $0.171 |
| plan | 34691260727 | 11:31:16 | **37 m 29 s** | ❌ failure (exit 2) | 3 (workflow 19/19 에이전트 완주) | `factory:ready → factory:needs-human` (script) | ❌ 없음 | **$11.95** |
| implement · review · merge · retro | — | — | — | 미도달 | — | — | — | — |

- 무개입 완주 **0/1**. 리뷰어 verdict R1→R2 뒤집힘(§12.4)은 **관측 불가**(review 미도달). revert 0.
- 손 라벨 차단 검증은 이 태스크 범위 밖(Task 4 이후).
- 환경 결함 없음(P6-R5) — 러너 `claude` 설치·토큰·gh 권한 모두 정상(triage 59 s 완주가 증거).

### 결함

| id | 분류 | sha | 한 줄 |
|---|---|---|---|
| **KTB-7** | KTB 프롬프트/프로토콜 | `516ac5a` + `9c3346d`(run-stage 부분) + `9c61315` | **디스패처가 workflow 결과를 요약해 스테이지가 통째로 날아갔다.** `factory-plan` 워크플로는 19/19 에이전트로 정상 완주했는데(`is_error:false`, `subtype:success`, 3,594 s), 설계가 "디스패처 세션의 **최종 텍스트** = workflow의 return 값"을 믿는 탓에 약 20 KB(~640줄) JSON을 모델이 다시 타이핑해야 했다. 모델은 "재타이핑 중 전사 오류 위험이 크다"며 `"r1": [ /* full R1 positions … */ ]`와 `"reason": "…"`로 **요약**했고, JS 주석 때문에 ```json 펜스가 `JSON.parse`에 실패했다. 수정: 산출물 출처를 **세션 트랜스크립트의 `Workflow` tool_result**로 올리고(신규 순수 모듈 `factory/lib/stage-artifact.js`의 `extractStageArtifact` — 트랜스크립트 → 펜스 → 맨 JSON 순으로 훑고 **스키마를 통과하는 첫 후보**가 이긴다), `run-stage.js`가 그 결과로 `.factory/out/<stage>.json`을 덮고 envelope은 `<stage>.envelope.json`으로 따로 남긴다. 디스패처 프롬프트 5개는 "생 JSON으로 통째로 그대로, 요약·축약은 **하드 실패**, 길이는 줄일 이유가 아니다"로 강화. 워크플로 템플릿 4개는 `~/.claude/projects/**/*.jsonl`을 아티팩트에 추가(트랜스크립트가 1순위 출처인데 업로드되지 않아 사후 조사가 불가능했다). 테스트: `stage-artifact.test.js`(신규 8건, 실패 페이로드를 픽스처로) + `verify-stage`·`templates`·`yml-lint` 확장. **재리뷰(this commit)**: ① 업로드 경로가 `~/.claude/projects/**/*.jsonl`이었는데 `actions/upload-artifact`는 glob만 다루고 `~`를 셸처럼 펼치지 않는다 — `if-no-files-found: ignore`까지 붙어 있어 **실패조차 없이 빈 아티팩트**가 올라가고 있었다(수정의 핵심이 몇 회차째 무효). 선행 스텝이 `CLAUDE_TRANSCRIPTS=$HOME/.claude/projects`를 `GITHUB_ENV`로 굳히고 경로를 `${{ env.CLAUDE_TRANSCRIPTS }}/**/*.jsonl`로 바꿨다. `yml-lint`에 `tilde-path` 규칙 추가. ② `bin/retro.js`가 아직 옛 `extractJson`을 쓰고 있었다 — `retro.v1`도 lessons·예시·관점·제안을 전부 싣는 큰 객체라 같은 방식으로 회차가 통째로 날아갈 수 있다. `extractStageArtifact` + 트랜스크립트 후보로 옮기고 `factory-retro.yml`에도 트랜스크립트 업로드를 더했다. ③ `bin/verify-stage.js`(진단 CLI)가 트랜스크립트를 아예 보지 않아 CI와 **다른 답**을 냈다 — `--transcript <path>`와 봉투 `session_id` 자동 탐색을 더하고, 경로 계산 `readTranscript()`를 `lib/stage-artifact.js`로 올려 세 진입점이 같은 계산을 쓰게 했다. 테스트: `verify-stage-cli.test.js`(신규 7건)·`stage-artifact.test.js`(readTranscript 5건)·`retro-bin.test.js`(4건)·`verify-stage.test.js`(end-to-end 1건)·`yml-lint.test.js`. |
| **D2** | KTB 코드 | `516ac5a` | `factory/lib/usage.js`의 `USAGE_RE`가 usage 블롭을 `\{[^}]*\}`로 잡아 **중첩** usage(`output_tokens_details`·`server_tool_use`·`cache_creation`·`iterations`)를 못 맞췄다. run 기록에 `cost_usd: 11.95`가 있는데 `factory status`는 `$0 · 1 runs · 0/0 tokens` — **ADR-005 소비 보고가 전면 무력**이었다. 탐욕 `\{.*\}`로 고쳐 실측 확인(`$12.120796 · 2 runs · 10/16104 tokens`). 테스트: `usage.test.js`에 실제 `claude -p` 페이로드 회귀 1건(기존 테스트는 전부 **평평한** usage만 써서 못 잡았다). |
| **D3** | KTB 코드 | `516ac5a` (+ KTB-7에 흡수) | `verify-stage.js`의 `extractJson`이 ```json 펜스 파싱 실패 시 조용히 균형-스캔 폴백으로 내려가 **계획 안의 done_when 한 항목**(`{id,text,verify,level}`)을 집어 왔다. 그래서 전이 사유가 "issue is required; tier is required; …"라는 2차 증상만 남기고 진짜 원인(펜스가 유효한 JSON이 아님)을 가렸다. 이제 후보별로 무엇이 어긋났는지 한 줄씩 적는다. |
| **KTB-8** | KTB 코드 / 워크플로 템플릿 | this commit | **스테이지 워크플로 5개가 하나의 concurrency 그룹을 공유해, 라벨 이벤트가 자기가 띄우려던 스테이지 런을 스스로 취소했다.** KTB-7 업그레이드 뒤 #2를 재큐하자 triage는 60 s에 통과했는데(`factory:queue → factory:ready`, $0.1525) 12:55:11Z 라벨 이벤트가 만든 런 5개가 서로를 밀어내며 **유일하게 조건이 맞는 `factory-plan`(34694986485)이 생성 1초 만에 취소**됐다. 이슈는 `factory:ready`에 런 기록도 코멘트도 없이 영구 정지 — 하트비트를 남긴 적이 없고 `blocked` 라벨도 없어 sweeper의 두 팔(`in-progress` stale, `blocked`) 모두에게 보이지 않는다. 메커니즘: `issues: labeled`에는 라벨 이름 필터가 없어(GitHub가 지원하지 않는다) 라벨이 한 번 바뀔 때마다 워크플로 5개가 **전부** 런을 만들고, `cancel-in-progress: false`에서도 GitHub는 그룹당 실행 1 + 대기 1만 유지하므로 새 런이 직전 대기 런을 취소한다. 1차 실행이 통과한 건 `factory-plan`이 우연히 마지막으로 그룹에 들어갔기 때문 — **레이스이지 결정론이 아니다**(1차 보고서 O3가 실현됐다). 수정 셋: ① concurrency 그룹을 워크플로별로 분리(`factory-issue-<n>-<stage>`) — 이슈 단위 상호배제는 concurrency가 아니라 `.factory/lib/claim.js`의 원자적 락 브랜치 claim이 이미 주고 있었고, 공유 그룹은 보호를 더하지 않으면서 스테이지를 죽이는 레이스만 만들고 있었다(yml 주석으로 명시). ② 스테이지 5개에 `workflow_dispatch: inputs.issue`를 달고 잡 조건이 라벨 이벤트 **또는** dispatch를 받게 했다(`run-stage.js`는 이미 argv에서 이슈를 읽는다; 이슈 번호는 `github.event.issue.number \|\| inputs.issue`) — 라벨이 이미 목적 상태에 있으면 같은 라벨을 또 붙여도 `labeled` 이벤트가 나지 않으므로 dispatch가 **유일한** 재점화 경로다. ③ sweeper 세 번째 팔: `ready\|planned\|awaiting-review\|approved`에 앉아 있고 마지막 전이 코멘트가 `staleMinutes`보다 오래됐으며 그 창 안에 하트비트도 재점화 마커도 없는 이슈를 `gh workflow run factory-<stage>.yml -f issue=<n>`으로 다시 띄우고 `<!-- factory-sweeper restarted stage=… issue=… -->`를 남긴다(마커가 dedupe의 근거; 하트비트 확인이 in-flight 런 조회를 대신한다 — 더 싸고 이미 읽는 데이터다). 사람·컨트롤러용 같은 손잡이로 `factory run <stage> <issue> --remote`(로컬 실행 없이 dispatch만, merge도 받는다). 테스트: `yml-lint.test.js`(그룹 5개 pairwise distinct + dispatch 트리거 2건)·`sweeper.test.js`(5건)·`run-cli.test.js`(3건)·`gh.test.js`. |
| **KTB-9** | KTB 코드 (= 아래 D1) | this commit | **tier 라벨을 붙이는 코드가 없었다.** 스펙 §3.2는 triage가 `factory:tier-docs\|standard\|load-bearing`을 부여한다고 적었고 `lib/label-catalog.js`가 셋을 정의하지만, `addLabels`는 retro 경로에서만 쓰였다 — 이슈 #2에도 tier 라벨이 없고 tier는 handoff JSON 안에만 있어 사람이 이슈 목록에서 볼 수 없었다. `run-stage.js`가 triage handoff가 **검증된 뒤**(검증 전의 tier는 에이전트 자기 신고다) `gh.setTierLabel`로 해당 tier를 붙이고 다른 `factory:tier-*`를 같은 호출에서 뗀다. `setFactoryLabel`을 재사용할 수 없다 — 그건 `STATES`만 보므로 태우면 상태 라벨이 떨어져 나간다(tier는 상태와 직교하되 한 번에 하나다). 라벨 실패는 기록만 남기고 스테이지를 죽이지 않는다(라벨은 사람에게 보이는 표식이고 게이트·로스터는 계속 handoff의 tier를 읽는다). `factory status`는 원래대로 handoff에서 읽는다. 테스트: `run-stage.test.js`(4건)·`gh.test.js`(2건). |
| **KTB-10** | KTB 코드 / 워크플로 템플릿 | this commit | **KTB-8·9 배치 리뷰 후속 — 중복 실행을 막는 것은 락이 아니었다.** KTB-8은 "중복 dispatch 자체는 무해하다(락 claim이 두 번째 러너를 fail closed 시킨다)"고 적었는데 거짓이다: 같은 concurrency 그룹에 PENDING으로 걸린 dispatch는 그룹당 실행 1 + 대기 1이라는 성질 때문에 **원래 런이 끝난 뒤에** 시작하고, 그때 락은 이미 풀려 있다. 그래서 sweeper/`--remote`가 민 런이 스테이지를 처음부터 다시 돌고(plan 한 번 ~$12 + 중복 handoff 코멘트), 전이 그래프는 그 **뒤에야** 거부한다. `run-stage.js`가 claim 직후(로컬 진입 §4.2.5 뒤) 이슈의 현재 상태 라벨이 그 스테이지의 진입 라벨(`ENTRY_LABELS`)인지 보고 아니면 전이·handoff·`claude -p` 없이 exit 0으로 물러난다(I2). 함께: ① 스테이지·retro 워크플로의 `CLAUDE_TRANSCRIPTS` resolve 스텝이 setup **뒤**에 있어, setup이 실패하면 `if: always()`인 업로드 경로가 `/**/*.jsonl`(루트 앵커 glob)로 접히고 `if-no-files-found: ignore`라 조용했다 — 스텝을 첫 스텝 + `if: always()`로 올리고 경로에 `|| format('{0}/.factory/out', github.workspace)` 폴백을 넣었으며, `${{ env.… }}`로 시작하는 artifact path에 `||`가 없으면 거부하는 yml-lint 규칙을 추가했다(I1). ② L1 거부 문구를 규칙별로 갈랐다 — additive-only 위반과 lessons 파일 삭제/이동이 각자의 제목·설명을 받고, 한 PR이 둘 다 어기면 두 섹션이 함께 실린다(I3). ③ sweeper 재점화 마커를 dispatch **전에** 남기고(코멘트 실패가 dedupe 근거를 지워 30분마다 다시 미는 것을 막는다, M4), back-pressure로 `factory:planned`에 세워 둔 이슈는 멈춘 것이 아니므로 dispatch도 코멘트도 하지 않는다(M5). ④ `factory run … --remote`가 argv 어느 위치의 플래그도 받고 `gh workflow run`에 `-R <repo>`를 명시한다 — 없으면 cwd의 git remote로 남의 레포를 띄운다(M7). ⑤ tier 라벨이 전이보다 먼저여야 하는 이유(라벨 이벤트의 5-런 물결이 다음 스테이지의 PENDING 런을 밀어낸다)를 주석·테스트로 고정(M2), yml-lint tilde 규칙이 따옴표를 벗기고 본다(M3), lessons 파일이 "읽히지 않음"과 "삭제됨"을 다른 사유로 낸다(M8). **M1(dispatch)은 라이브로 확인됐다** — run `34696709110`(`factory-plan`, `workflow_dispatch`, non-skipped)이 실제로 떴다. 테스트: `run-stage.test.js`(진입 가드 6건 + tier 순서)·`yml-lint.test.js`(새 규칙 + 따옴표 + 템플릿 스텝 순서)·`sweeper.test.js`(마커 순서 2건 + back-pressure 2건)·`merge-stage.test.js`(규칙별 제목 3건)·`integrity.test.js`(unreadable)·`run-cli.test.js`(`-R` + 플래그 위치 3건). |
| **D1** | KTB 코드 | KTB-9에서 수정 | 스펙 §3(line 98)은 `factory:tier-docs/standard/load-bearing`을 "triage가 부여"한다고 적었는데 **부여하는 코드가 없다**. `bootstrap`이 라벨 3개를 만들기만 하고 `addLabels`는 retro 경로에서만 쓰인다 — 이슈 #2에도 tier 라벨이 없고 tier는 handoff JSON 안에만 있다. 사람이 이슈 목록에서 tier를 볼 수 없다. |

### 데모 반영

**BLOCKED_ON_UPGRADE.** 수정이 전부 `.factory/**`·`.claude/**`·`.github/workflows/factory-*.yml`
(보호 경로)에 떨어져 데모 `main`은 `factory init --upgrade` PR로만 받는다. 데모 PR은 열지 않았다
(PR #3이 이미 대기 중). 업그레이드가 착지한 뒤 `:unstick`
(`transition.js 2 factory:queue --human`)으로 #2를 재큐한다.
**이번에 재큐하지 않았다** — 원인이 결정론적이라 그대로 다시 돌리면 같은 자리에서 $11.95를 또 태운다.

### 남은 관찰

- **O1** — plan이 스스로 사람 개입을 선행 조건으로 걸었다: `pg` 의존성 추가가 `package.json`(보호 경로)에
  걸려 "`pg` 한 줄만 담은 `factory:harness` PR을 **사람이** 머지한 뒤 #2를 claim한다". 그린필드 첫 기능
  이슈가 사람 머지를 요구하는 건 §11 흐름의 실제 마찰 — 픽스처가 의존성을 미리 깔지, `[protected]`를
  tier별로 열지 판단이 필요하다.
- **O2** — `docs/features/001-create-note.md`의 `issue: null`을 채우지 못했다(`main` 보호, 로컬 push 불가).
  컨텍스트 로더가 이슈 본문의 `Spec: …` 줄에서 경로를 뽑으므로 파이프라인 영향은 없었다.
- **O3** — 라벨 이벤트 하나에 워크플로 **5개**가 뜬다(`issues: labeled` + 같은 concurrency 그룹).
  관측된 3회 모두 올바른 스테이지가 슬롯을 잡았지만, `cancel-in-progress: false`에서는 나중에 들어온
  pending이 먼저 들어온 pending을 취소하므로 한 스테이지가 라벨을 두 번 건드리면 진짜 런이 시작 전에
  취소될 수 있는 **잠재 레이스**다(이번엔 미발현).
- **O4** — 세션 트랜스크립트가 아티팩트에 없어 원인 추적을 `.factory/out/plan.json`의 `result` 역추적으로
  해야 했다. KTB-7 수정에서 업로드 경로 추가 — **그 경로가 `~`로 시작해 실제로는 아무것도 올리지
  않았다**(재리뷰에서 발견, 위 KTB-7 행 ①). "고쳤다"와 "동작한다" 사이에 러너에서의 확인이 없었다.
  KTB-10에서는 그 간극을 한 번 메웠다 — KTB-8의 dispatch 손잡이(M1)가 실제로 뜨는지를 run
  `34696709110`(`factory-plan`, `workflow_dispatch`, non-skipped)으로 라이브 확인했다. 나머지
  항목(경로 폴백·가드)은 여전히 테스트로만 검증됐다.
- **O5** — `factory status`가 `_retro.md` 상태 파일을 이슈처럼 센다(`- #_retro $0 · 0 runs`). 표시 잡음.
- **O6** — **락 프로토콜이 한 번 깨졌다.** 내가 락을 반납한 사이 다른 에이전트가 `factory/bin/run-stage.js`의
  내 **미커밋** 변경을 자기 KTB-6 커밋(`9c3346d`)에 함께 담았고, 그 결과 HEAD가 아직 untracked인
  `factory/lib/stage-artifact.js`를 import하는 상태가 잠시 존재했다. 명시 경로 `git add`만으로는
  같은 파일을 둘이 건드릴 때를 못 막는다 — 파일 단위 소유권이나 워크트리 분리가 필요하다.
- **O7** — heartbeat 정상(10분 간격, 12:01:34 갱신 확인). 37분짜리 스테이지가 죽었는지 살았는지 구분됐다 —
  sweeper 설계의 전제가 실제로 성립한다.
- **O8** — **취소된 런은 흔적을 남기지 않는다.** KTB-8의 plan 런은 `cancelled`로 1초 만에 끝나 잡 로그도
  아티팩트도 코멘트도 없었다. "무슨 일이 있었나"를 알 수 있는 유일한 창구가 `gh run list`이고, 그것마저
  90일이 지나면 사라진다. 재점화 마커 코멘트(KTB-8 ③)가 최소한 "스위퍼가 밀었다"는 사실은 이슈에
  남기지만, **취소 자체는 여전히 이슈에 기록되지 않는다** — 사후 조사는 계속 Actions 로그에 의존한다.
- **O9** — **업그레이드 커밋 제목이 실제 배포된 KTB sha를 말해야 한다.** 데모 PR #3의 제목은
  `init --upgrade to KTB f12a174`였지만 내용은 f8b0439까지였다(`.factory/lib/stage-artifact.js` 존재로
  확인). 제목의 sha를 믿고 "그 수정은 아직 안 갔다"고 읽으면 같은 결함을 두 번 조사하게 된다 —
  업그레이드 PR을 만들 때 제목의 sha는 `git rev-parse HEAD`로 **그 자리에서** 찍어야 한다.

## Task 4 — 브라운필드 doctor 점검(KTB-11) + own-calendar 준비에서 나온 설계 공백(G1)

### 결함

| id | sha | 한 줄 |
|---|---|---|
| **KTB-11** | this commit | **`files.stale`이 병합 대상 파일까지 바이트로 비교해 정당한 커스터마이즈를 영구 WARN으로 오보했다.** 브라운필드 저장소(사람이 `.claude/settings.json`에 자기 deny/allow 항목을 추가해 둔 상태)에서 `factory doctor`를 돌리면 그 파일이 `manifest.js`의 `merge: "settings"` 대상인데도 `checkFiles`가 템플릿 렌더 결과와 **바이트**로 비교해, 사람의 정당한 추가분이 있는 한 `run factory init --upgrade`로도 절대 지워지지 않는 `files.stale` WARN이 상시로 떴다. `checkFiles`가 `merge: "settings"` 항목만 `mergeSettings(installed, template)`(`factory/cli/install.js`의 기존 순수 함수 재사용)로 다시 판정하도록 고쳤다 — 병합 결과가 설치본과 다를 때만(=템플릿 deny/allow/훅 항목이 실제로 빠졌을 때만) stale이다. 테스트: `doctor-factory.test.js`(customized-but-complete → PASS · 템플릿 훅 누락 → WARN · 바이트 동일 → PASS 회귀). |

### 설계 메모 — G1: `[runtime].setup`으로 Node 외 툴체인을 깔 수 있는가

own-calendar 준비 중 나온 질문: 러너 셋업(`.factory/actions/setup/action.yml`)은 `actions/setup-node` +
`[runtime].setup`만 돌므로, Flutter나 Python처럼 Node가 아닌 툴체인이 필요한 저장소는 그것을 표현할 자리가
없다. 전용 `[runtime].actions`(다중 스텝) 메커니즘은 지금 범위에서는 만들지 않고 뒤로 미룬다 — 대신
`[runtime].setup` 한 줄로 툴체인 설치 + `$GITHUB_PATH` 확장을 표현하는 것을 정식 경로로 문서화했다
(`harness.toml`의 `setup_note`, Flutter 예시 포함) — 그리고 그 경로가 실제로 성립하도록 `[runtime].setup`
호출을 자기만의 액션 스텝으로 분리했다(npm install/git config와 섞여 있으면 PATH 확장 경계가 파일만 보고는
드러나지 않는다). 자세한 판결은 ADR-020 G1. 알려진 한계: 한 줄짜리 셸 명령으로 되는 설치까지만 연다 —
스텝별 캐싱이나 조건부 다중 설치가 필요해지면 그때 `[runtime].actions`를 다시 본다.

## Task 4 (계속) — L2 부여의 붕괴 외 (KTB-13~17, 라운드 3)

번호 정정: 이 섹션은 계획의 **Task 4**(이슈 4건 다크 처리 + 건너뛰기 차단 + 로컬 경쟁, 데모 #2·#8 계속)에서 나온 결함이다 — 계획의 Task 5는 KTB 자기 dogfood(다른 대상, 다른 로그 `docs/factory/dogfood/2026-09-12-ktb.md`)를 가리키므로, 이 파일(데모 전용)에는 "Task 5" 제목이 있으면 안 된다. 원래 초안이 순서대로 append하다 번호를 잘못 매겼다(Task 7 문서 정리에서 바로잡음).

### 결함

| id | sha | 한 줄 |
|---|---|---|
| **KTB-13** | this commit | **`dontAsk`가 allow에 없는 도구를 승인이 아니라 거절로 처리해, builder가 파일을 한 줄도 쓸 수 없었다.** CI는 `claude -p /factory-<stage> <n> --permission-mode dontAsk --settings .factory/ci-settings.json`으로 돈다. 설치된 `.claude/settings.json`의 `permissions.allow`에는 Bash 패턴 8개만 있고 **도구 이름은 한 줄도 없었다** — 설계가 ADR-002/ADR-008의 스파이크 관측("`dontAsk` 단독으로 모든 툴 호출이 프롬프트 없이 통과, `permission_denials: []`")을 믿었기 때문이다. 그 전제가 현행 CLI에서 뒤집혔다: 지금의 `dontAsk`는 allow에 걸리지 않는 호출을 **묻지 않고 거절**한다. **데모 #2 implement 재작업 라운드**의 builder가 그대로 보고했다 — "Write and Edit tools are denied by the sandbox (dontAsk mode), and bash file writing (redirection, heredoc into a file) is denied too. Creating files through git apply is accepted only for trivial…" — 그리고 포기해 `factory:needs-human`으로 갔다. **데모 #8**은 성공했지만 그 방식이 증상 자체다: builder가 편집을 `git apply`에 실어 밀어 넣었다(`Bash(git *)`가 allow에 있었으므로) — 좁은 allow가 곧 차단 목록이었고 에이전트는 그 구멍을 찾는 법을 배우고 있었다. 수정: `templates/factory/claude/settings.json`의 `permissions.allow`가 팩토리 에이전트의 도구를 실제로 부여한다(`Read`·`Edit`·`Write`·`MultiEdit`·`NotebookEdit`·`Glob`·`Grep`·`LS`·`Agent`·`Workflow`·`TodoWrite`·`Bash(*)`; 좁은 Bash 8개는 대체 — `mkdir`·`cat > file`·`sed -i`가 전부 막히고 있었다). deny(두 파일)·훅·integrity는 **한 줄도 건드리지 않았다** — 넓어진 것은 부여이지 방벽이 아니다: deny가 allow를 이기고, `block-dangerous.sh`가 `Bash(*)`의 실질 경계이며, 쓰기 금지 역할은 `deny-all-writes.sh`가 막는다(PreToolUse 훅은 permission 판정보다 **먼저** 돌고 exit 2가 호출 자체를 차단하므로, 전역 allow가 넓어져도 리뷰어는 쓸 수 없다). doctor에 `settings.allow`를 더해 템플릿 항목 누락을 `settings.deny`와 같은 무게의 FAIL로 본다. 기존 설치는 `mergeSettings`의 합집합으로 `init --upgrade`에서 새 항목을 얻는다(KTB 자신에게 적용해 멱등 확인; 뒤에 남는 좁은 Bash 항목은 `Bash(*)`가 포함하므로 무해). 판결은 ADR-020 KTB-13 — ADR-002/ADR-008의 "`dontAsk`가 전부 승인한다"는 관측을 현행 CLI에 대해 철회한다. 테스트: `templates.test.js`(allow 부여 목록 + 좁은 Bash 부재)·`doctor-factory.test.js`(`settings.allow` 누락 FAIL·상위집합 PASS·allow 부재 FAIL)·`hooks.test.js`(순서 근거 주석). |
| **KTB-13 r1** | this commit | **리뷰 지적: "훅이 `Bash(*)`를 덮는다"가 검증되지 않은 문장이었다.** 좁은 allow가 사실상 두 번째 방벽이었다 — `node`·`curl`·`wget`·`install`은 allow에 없어서 **훅까지 도달한 적이 없고**, 그래서 두 훅 중 어느 쪽도 그 모양을 판정해 본 적이 없다. allow를 열자 그것들이 처음으로 훅 앞에 섰다. `deny-all-writes.sh`: `MultiEdit`을 Edit/Write 팔에 넣고, Bash arm에 `node -e\|-p\|--eval\|--print`(대상 불문), `curl`의 출력 플래그, `wget` 전면(플래그가 없어도 cwd에 파일을 만든다), `install`, `cp`/`mv`의 `-t`/`--target-directory`(목적지를 마지막 토큰이 아닌 곳에 두어 기존 규칙을 통째로 우회했다)를 추가. `block-dangerous.sh`: 보호 경로에 대한 `node` 인라인 스크립트·`dd of=`·`install`·`curl`/`wget`·`git checkout <ref> -- <path>`/`git restore <path>`, 그리고 `git apply`/`git am` **전면 차단**(패치 내용이 명령줄에 없어 훅이 무엇이 쓰이는지 볼 수 없다 — 판정 불능은 안전이 아니다; 데모 #8이 쓴 우회로가 여기서 닫힌다. builder는 이제 Edit/Write로 직접 쓰므로 손실 없다). 매처는 쓰기 금지 역할 14개 전부 `Edit\|Write\|MultiEdit\|NotebookEdit\|Bash`이고, `lintAgentMd`의 `deny-hook`이 다섯 이름 전부를 요구하며 무엇이 빠졌는지 말한다(`DENY_WRITES_TOOLS`가 정본). doctor의 `settings.deny` FAIL도 `settings.allow`와 같은 `init --upgrade` 안내를 싣는다. **잔여 위험은 ADR-020 KTB-13 r1의 등록부에 표로 고정했다** — 훅은 명령 모양의 **열거**이지 화이트리스트가 아니고(`rsync`·`tar -x`·`ruby -e`… 미포함), reviewer·verifier·loader도 Bash에 닿으며, L1은 **PR에 도달한 diff만** 본다(그들은 커밋하지 않으므로 워크트리 변조는 어떤 diff에도 나타나지 않는다). 테스트: `hooks.test.js`(block-dangerous 19 blocked / 12 allowed, deny-all-writes 18 blocked / 10 allowed, MultiEdit 3 — r2에서 붙은 옵션 회귀가 더해진다)·`agent-md.test.js`(매처 누락별 위반)·`doctor-factory.test.js`(deny 안내 문구). |
| **KTB-13 r2** | this commit | **재리뷰 지적(High): r1의 curl·cp/mv 규칙이 한 글자 차이로 열려 있었다.** 그 규칙들은 플래그 뭉치 뒤에 공백이나 `=`를 요구했는데(`([[:space:]=]|$)`), 짧은 옵션은 값을 **붙여** 받는다 — 셸이 한 토큰으로 넘기고 도구는 정상 동작한다. `curl -o.factory/harness.toml u`, `curl -sLosrc/a.js u`, `cp -tsrc/sub a`, `mv -tdir a`가 전부 exit 0으로 통과했다. 즉 r1이 "막았다"고 적어 둔 모양이 실제로는 막히지 않았다(`block-dangerous.sh`는 보호 경로 **부분 문자열**을 보므로 영향 없음 — 그래도 회귀 테스트는 양쪽에 넣었다). 수정: 공용 조각 `ATTACHED='[a-zA-Z]*[^[:space:];&\|]*'`를 뭉치 뒤에 붙여 **플래그 글자가 뭉치 안에 있다는 사실**로 판정한다 — 값이 붙어 있든 떨어져 있든 같은 규칙이 선다. 넓어진 대가는 오탐 쪽으로만 간다: 출력 플래그 없는 `curl -sSL`·`curl -H 'x: y'`·`curl --location`, `cp -a`·`cp -r`은 그대로 통과(테스트로 고정). 같은 가족의 `node -e"code"`·`sed -i.bak`은 이번 범위가 아니다 — 관측된 적이 없어 ADR-020 등록부 gap 1("관측될 때마다 자라는 목록")에 그대로 둔다. ADR-020 KTB-13 r2의 알려진 오탐 목록에 `git apply --check`/`--stat`/`--numstat`도 등재했다(적용하지 않는 읽기지만 `git apply` 전면 차단에 걸린다 — 플래그 하나로 차단을 끄는 경로를 여는 것이 더 나쁘다). 테스트: `hooks.test.js`(deny-all-writes 붙은 형태 8 blocked / 7 allowed, block-dangerous 3 blocked / 2 allowed). |
| **KTB-14 r1** | this commit | **재리뷰 지적(Medium): 판정 불가를 needs-human으로 부르고 있었다.** KTB-14의 워크트리 백스톱은 `git status` 자체가 실패한 경우(`clean.ok === false` + `dirty` 없음)도 `factory:needs-human`으로 보냈다. 이 저장소의 계약은 반대다 — `merge-stage.js`의 `undecidable()`과 게이트 BLOCKED이 정한 대로 **판정 불가는 `factory:blocked`**이고, `needs-human`은 "사람이 판단할 것이 있다"는 뜻이다. `git status`가 실패했으면 사람이 볼 재료조차 없고 원인은 대개 러너 쪽이라 재시도로 풀린다. 수정: 더러운 트리만 `needs-human`, `git status` 실패는 `blocked`(어느 쪽이든 산출물은 받지 않는다 — `verifyStage`조차 부르지 않는 것은 그대로). 이 등급이 실제로 성립하려면 그래프에 `factory:queue → blocked`·`factory:ready → blocked`가 있어야 한다(KTB-16이 같은 이유로 더했다) — 없으면 triage·plan은 blocked를 요청했다가 거부당해 라벨이 그대로 남는다. 덤: `pathsOfStatusLine`의 docstring에 `core.quotePath`의 C 인용 경로 한계를 적었다(인용된 경로는 스크래치 접두와 절대 일치하지 않아 항상 "더러움"으로 세므로 **판정 방향에서는 안전한 쪽으로 틀린다**; 정확한 파싱이 필요해지면 `-z`가 정공법이다). 테스트: `run-stage.test.js`(git-status 실패 → blocked, needs-human 아님). |
| **KTB-15** | this commit | **draft PR은 절대 머지될 수 없었다 — 파이프라인이 merge까지 처음 도달해서야 드러났다.** 데모 #8은 triage·plan·implement·review를 전부 통과하고 merge에서 40초 만에 죽었다: `gh pr merge failed (1): GraphQL: Pull Request is still a draft` → `factory:approved → factory:blocked`. implement 워크플로는 `gh pr create --draft`로 PR을 열고(`factory-implement.js:263`, `gh.js`의 `createDraftPr`) 파이프라인 어디에도 ready로 되돌리는 코드가 없었다. 게이트·리뷰·무결성·정책을 전부 통과해도 마지막 한 걸음에서 GitHub이 거부한다. 수정(관측자 A안): draft로 여는 것은 **유지하고**(리뷰 중인 PR을 사람이 실수로 머지하는 것을 막는 신호다) `gh.prReady(pr)`(`gh pr ready`)를 `merge-stage.js`가 **`mergePr` 바로 앞**에서 부른다 — 보호 경로·역할 섹션 정책·게이트·필수 체크·무결성이 전부 통과한 뒤다(그 앞에 두면 거부된 PR이 ready로 남아 사람이 실수로 머지할 수 있다). 이미 ready인 PR에도 exit 0이라 멱등이다. 실패는 머지 실패와 같은 등급(`factory:blocked` — 아직 머지되지 않았고 재시도로 풀린다). 더해서 전이 그래프에 **`factory:blocked → factory:approved`**: 머지만 실패한 런은 고칠 것이 코드가 아니므로 planned로 되돌려 이미 GREEN인 구현을 다시 돌 이유가 없다(요구조건은 그대로 — review handoff·이번 런의 GREEN `gates.json`·PR head 일치). sweeper의 blocked 팔은 **그대로** 유예 뒤 needs-human으로 올리고, 사람 경로(`transition.js <n> factory:approved --human` → `factory run merge <n> --remote`)를 sweeper 주석과 `:unstick` 선택지 목록에 적었다. 채택하지 않은 것: `still a draft` 문자열로 등급을 재분류하는 안(원인을 고친 뒤에는 나올 자리가 없고, GitHub 메시지 변경에 그대로 깨진다). 알려진 마찰: 사람 경로의 전이도 `gatesGate`의 GREEN `gates.json`을 요구하므로 러너 밖에서는 거부된다 — ADR-020 KTB-15에 남겼다. 테스트: `merge-stage.test.js`(호출 순서, 거부된 머지는 draft를 건드리지 않음, prReady 실패 → blocked, dep 부재), `gh.test.js`, `labels.test.js`. |
| **KTB-16** | this commit | **`--max-turns 5`가 plan을 죽였다 — 30분·$12.05가 산출물 없이 증발했고, 사유는 원인을 숨겼다.** KTB-13 배포 후 재큐된 #2의 plan 재실행(run 34700674634)이 `is_error: true`·`subtype: error_max_turns`·`terminal_reason: max_turns`·`num_turns: 6`으로 끝났다. `orchestration = "workflow"`에서 `Workflow`는 **백그라운드**로 돌아(KTB-17) 디스패처가 쓰는 턴은 최소 ① 호출 ② 접수증 ③ 완료 알림 ④ 출력이고, 알림의 `<result>`가 잘려 output 파일을 `Read`로 읽어야 하면 조각 수만큼 더 붙는다. 직전 실행은 3턴이었다 — 경계에 걸려 있었다. 더 나쁜 것은 이슈에 남은 사유가 "no JSON object in result"(증상)라 사람이 프롬프트를 의심하게 만든다는 점이다. 수정: ① 한도를 하네스로 뺐다 — `[factory].max_turns`(기본 **12**, 관측값: 네 턴 + 조각 4 + 여유) + `[factory.max_turns_by_stage].<stage>`, `loadHarness`가 기본값을 채우고 `stageMaxTurns()`가 읽는다(정수가 아니면 조용히 쓰지 않고 기본값). ② doctor의 `factory.max_turns`가 정수 **3–50**을 강제(오타로 12가 120이 되면 잘못된 프롬프트가 몇 시간·수백 달러를 태운다), 키가 없으면 WARN + `init --upgrade` 안내. ③ run 기록/전이 사유의 첫 줄이 `claude -p hit max turns (<n>)`이고 스키마 진단은 그 뒤에 붙는다(`terminal_reason`·`subtype` 둘 다 본다 — CLI 버전에 따라 한쪽만 온다). ④ 등급은 `needs-human`이 아니라 **`factory:blocked`**(재시도로 풀리는 일시 조건 — gates BLOCKED·머지 API 실패와 같은 자리). 그래서 `factory:queue → blocked`·`factory:ready → blocked` 엣지를 더했다: 없으면 triage·plan은 blocked를 요청했다가 거부당해 라벨이 그대로 남는다. ⑤ **단, 트랜스크립트를 먼저 본다**(KTB-17) — 복구하면 그 런은 성공이고 게이트도 그때는 돈다. 테스트: `run-stage.test.js`(표·등급 2종·복구 성공+게이트 실행), `verify-stage.test.js`, `doctor-harness.test.js`, `labels`/`config`. |
| **KTB-17** | this commit | **KTB-7의 트랜스크립트 폴백은 접수증을 보고 있었다 — 계획은 트랜스크립트 안에 온전히 있었다.** `Workflow` 툴은 백그라운드로 돌기 때문에 그 tool_result는 반환값이 아니라 `"Workflow launched in background. Task ID: … Transcript dir: …"` 접수증이다(관측자가 라운드 3 아티팩트로 재생해 확인: Workflow tool results 1건, 1,340자, 전부 접수증). 실제 반환값은 한참 뒤 **USER 텍스트 블록** `<task-notification>…<status>completed</status><output-file>…</output-file><result>{raw JSON}</result></task-notification>`로 온다. 실제 트랜스크립트(run 34700674634, 60줄)를 열어 두 가지를 더 확인했다: (a) 알림의 `<result>`는 **잘린다** — 45 KB가 8,179자 + `"... (truncated 36707 chars, full result in /tmp/…/w6xdqhynw.output)"`로 왔다(유효한 JSON이 아니다), (b) 그래서 디스패처가 읽는 output 파일은 한 번에 오지 않는다 — 525줄이 `offset`/`limit`으로 **네 조각**(1–129, 130–329, 330–523, 524–525)으로, 각 조각은 `N\t` 줄 번호가 붙은 텍스트이고 **어느 조각도 단독으로는 유효한 JSON이 아니다**. 수정(후보 순서 교체 + 재조립): ① completed인 마지막 `<task-notification>`의 `<result>` → ② `Read` tool_result를 **파일별로 줄 번호 순서대로 재조립**한 내용, 그다음 개별 tool_result(둘 다 `^\d+\t` 접두를 벗기고, 바깥에 `summary`·`agentCount`·`logs`가 있으면 `result`/`data`를 **한 겹만** 벗긴다) → ③ `Workflow` tool_result — **접수증이 아닐 때만**(접수증이면 그 사실을 사유에 남긴다) → ④⑤ 봉투의 펜스/맨 JSON. 후보는 전과 같이 전부 스키마로 채점한다. 큰 텍스트에는 전체 균형 스캔 대신 선두 객체 하나만 본다(100 KB tool_result마다 O(n·중괄호수)). **검증**: 실제 아티팩트에 `verify-stage` CLI를 그대로 돌려 `{"ok":true,"reasons":[],"transcript":"…/d6799532-….jsonl"}` — 수정 전 그 런의 기록은 `verify: FAIL / - claude -p reported is_error / - no candidate matched the stage schema — no JSON object in result`였다. 그 트랜스크립트를 다듬은 픽스처(`factory/test/fixtures/plan-max-turns.jsonl`, 구조 그대로·토론 산문만 축약)가 테스트에 들어 있다. 테스트: `stage-artifact.test.js` 6건. |

### 라운드 3 스테이지 표 (관측자 보고서에서 옮김 — KTB-15/16/17의 1차 증거)

데모 main `c4bc237`(KTB `140ef1d`: 워크플로별 concurrency, 5개 스테이지 `workflow_dispatch`,
sweeper 재점화 팔, tier 라벨 부여, 트랜스크립트 업로드) 위에서 #2·#8을 병렬 관찰했다.

**이슈 #2 "001 Create note"** (spec 001, tier standard)

| 스테이지 | run id | 시작(UTC) | 종료 | 소요 | 결과 | 라벨 전이 | 비용 |
|---|---|---|---|---|---|---|---|
| plan (dispatch) | 34696709110 | 13:32:25 | 14:13:58 | 41 m 33 s | ✅ `verify: ok` | `ready → planned → in-progress` | $12.43 |
| implement | 34698672643 | 14:13:51 | 14:38:39 | 24 m 48 s | ❌ `gates RED: failing=prove-test` | `in-progress → needs-human` | $8.13 |
| plan (재실행) | 34700674634 | 14:55:10 | 15:25:28 | 30 m 18 s | ❌ `max_turns`(KTB-16) | — | $12.05 |

implement의 RED는 게이트가 옳았다 — `head_sha == base`, 즉 **변경이 0바이트**였다(KTB-12/13의 쓰기
채널 부재). prove-test는 빈 변경을 정확히 거부했다. plan 재실행은 6턴째에 잘렸고, 계획은
트랜스크립트 안에 있었는데 폴백이 접수증을 보고 있었다(KTB-16 + KTB-17 — 둘 다 이 커밋에서 닫힌다).

**이슈 #8 "healthz response is cacheable"** (버그, `:issue` 형식, 스펙 파일 없음)

| 스테이지 | run id | 시작(UTC) | 종료 | 소요 | 결과 | 라벨 전이 | 비용 |
|---|---|---|---|---|---|---|---|
| triage | 34696731588 | 13:32:54 | 13:34:00 | 66 s | ✅ | `queue → ready` + `tier-standard` | (기록 참조) |
| plan | 34696776886 | 13:33:53 | 14:02:04 | 28 m 11 s | ✅ `verify: ok` | `ready → planned → in-progress` | $7.78 |
| implement | 34698085338 | 14:01:59 | 14:39:53 | 37 m 54 s | ✅ gates GREEN 4/4 | `in-progress → awaiting-review` | $17.07 |
| review | 34699940863 | 14:39:48 | 15:23:37 | 43 m 49 s | ✅ `verify: ok` · gates GREEN 2/2 · checkout `4c2a8c7` | `awaiting-review → approved` | $11.33 |
| merge | 34702088997 | 15:23:31 | 15:24:11 | 40 s | ❌ `still a draft`(KTB-15) | `approved → blocked` | $0 (script-only) |

PR **#10 `claude/fq-8`** — +106/−3, 파일 3개, 보호 경로 0건, `additive_only` 위반 0건,
`factory/integrity` ✅. 보호/정책 검사는 깨끗했고 **머지 한 걸음에서만** 죽었다 —
#8은 merge를 빼고 전 스테이지를 통과한 첫 이슈다. 파이프라인 본체는 동작한다.

**라운드 3 소각**: 약 **$56.7**(위 다섯 스테이지) + 재실행 $12.05. 무개입 완주 0/2.
그 밖의 관측(훅-우선 순서 실물 확인, R1→R2 뒤집힘 표본 0, 라운드 3 비용)은 ADR-020
"라운드 3 관측 사실"에 판결 근거로 고정했고, 스테이지가 도는 중의 업그레이드 금지는 O11이다.

### 라운드 4 — API 쿼터 소진이 세 스테이지를 동시에 죽였다 (KTB-22)

| 이슈 | 스테이지 | run 종료(UTC) | 소요 | 결과 | 라벨 전이 |
|---|---|---|---|---|---|
| #2 (라운드 4 재실행) | implement | 20:20 | 299 ms | ❌ `api_error` 429 | `in-progress → blocked` |
| #15 (harness 승격) | implement | 20:20 | 299 ms | ❌ `api_error` 429 | `in-progress → blocked` |
| #18 | plan | 20:20 | 299 ms | ❌ `api_error` 429 | `ready → blocked` |

셋 다 같은 봉투다 — `is_error:true, subtype:"success", api_error_status:429,
terminal_reason:"api_error", num_turns:1, duration_ms:299, result:"You've hit your org's monthly
spend limit · ask your admin to raise it at claude.ai/admin-settings/usage · your session limit
resets 8:30pm (UTC)"`. 조직의 월 지출 한도가 찼을 뿐이고 1턴·299 ms 만에 죽었으니 트랜스크립트에
산출물이 있을 리 없다 — 그런데 `run-stage`/`verify-stage`는 사유를 "claude -p reported is_error;
no candidate matched the stage schema — transcript: the Workflow tool result is a background
receipt…"로 적고 `factory:needs-human`으로 올렸다. 사람이 볼 산출물도, 고칠 프롬프트도 없는데
"사람이 판단하라"는 라벨이 붙은 셈이다. 판결: KTB-22(ADR-020) — 등급은 `factory:blocked`, 사유는
프로바이더 메시지 원문, sweeper는 이 origin(사유에 "api error")에 한해 3번까지 재시도한 뒤에야
사람에게 넘긴다.

### 라운드 6 — 쿼터 회복 뒤 첫 완주 시도, 머지 0건 (KTB-23·24·25·26)

쿼터는 02:47Z(09-13)에 풀렸다. 세 이슈가 다시 돌았고, 셋 다 서로 다른 이유로 멈췄다.

| 이슈 | 스테이지 | run id | 소요 | 결과 | 라벨 전이 | 비용 |
|---|---|---|---|---|---|---|
| #15 | triage | 34733953513 | 2 m 01 s | ✅ `ready` + `harness` 유지 | `queue → ready` | — |
| #15 | plan | 34734032488 | 42 m 16 s | ✅ | `ready → planned → in-progress` | $16.07 |
| #15 | implement (harness 변형) | 34735751829 | 21 m 26 s | ✅ gates GREEN 4/4 · PR #20 (+709/−19, 보호 경로 2개) | `in-progress → awaiting-review` | $9.78 |
| **#15** | **review** | **34736609544** | **45 m 19 s** | ❌ **`cancelled` — `timeout-minutes: 45`** · 전이·기록·코멘트 **전무**, `refs/heads/factory/lock-15` **고아** | — (라벨 그대로) | 기록 없음 |
| #2 | plan | 34736457575 | 40 m 16 s | ✅ `verify: ok` | `ready → planned → in-progress` | $14.57 |
| #2 | implement | — | ~30 m | ❌ `transition refused: verifier rejected` (gates GREEN 4/4) — dw1/dw3/dw4가 `pg` 패키지를 요구하는데 `package.json`이 보호 경로 | `in-progress → needs-human` | $11.25 |
| #18 | review R2 | 34736025917 | 24 m 27 s | ✅ `verify: ok` · gates **GREEN** · `test-env: re-up ok` | `awaiting-review → rework` | $7.75 |
| #18 | implement R2 | — | — | ✅ | `rework → in-progress → awaiting-review` | $4.34 |

**라운드 6 소각 ≈ $69, 머지 0건.** 누적 머지는 여전히 #8 한 건.

- **KTB-21 해결 확인.** #18의 review가 `test-env: re-up ok`와 함께 gates GREEN으로 끝났다 — 라운드 5에서
  같은 이슈를 죽인 "리뷰어가 컴포즈를 내린 뒤 죽은 env에 대고 돈 게이트"가 재현되지 않았다.
- **KTB-23** — #2: builder가 `package.json`을 못 건드려 네 라운드가 "Harness change needed" 산문 →
  verifier reject → needs-human으로 끝났다(≈$67 소각). 판결: implement handoff에 `harness_needed[]`를
  두고, 그것이 차 있으면 `factory:harness` 이슈를 **하나** 열고 피처 이슈는 `needs-info`로 주차한다
  (ADR-020 KTB-23). 당시 수동 우회는 PR #24(사람이 `pg`를 추가해 머지) 뒤 #2 재큐였다.
- **KTB-24** — #15의 review는 **사람이 취소한 것이 아니다**(관측자는 이 세션에서 `gh run cancel`을 한 번도
  부르지 않았다). 45 m 19 s는 `factory-review.yml`의 `timeout-minutes: 45`에 정확히 걸린 시간이고, 잡
  로그의 "The operation was canceled."는 사람의 취소와 구분되지 않는다. 판결: 상한 상향(review 45 → 90,
  plan 60 → 75, triage 15 → 20, merge 20 → 30, retro 30 → 45) + `if: cancelled() || failure()` 정리
  스텝(기록 한 줄 · in-flight 라벨이면 `factory:blocked` · 락 해제)(ADR-020 KTB-24).
- **KTB-25** (관측 O18) — #18은 `needs-human`에서 재큐돼 triage부터 통째로 다시 돌았는데 그 **첫 리뷰**가
  `round: 2`였다. 라운드를 이슈 이력 전체의 review handoff 개수로 셌기 때문이다 — K=3 중 2를 이미 쓴 채
  시작했다. 판결: 마지막 `→ factory:queue` 전이 이후만 센다(ADR-020 KTB-25).
- **KTB-26** — sweeper cron이 또 믿을 수 없었다(라운드 5의 몇 시간 공백에 이어, 취소된 sweeper 런이 #15를
  30분 넘게 방치). 판결: 모든 스테이지·retro 워크플로의 마지막 스텝이 `sweep.js --quick`을 돈다
  (cron은 유지 — 둘은 겹이다)(ADR-020 KTB-26).
- **#15의 보호 경로 L1 머지 거부는 이번에도 관측하지 못했다** — review가 타임아웃으로 죽어 merge 스테이지에
  도달하지 못했다. 다음 라운드 1순위로 남는다.

**리뷰 라운드 교정 (ADR-020 KTB-23/24 r1, 소스 대조)** — 위 네 판결을 구현 뒤 소스에 대고 다시 읽어
네 개의 결함이 나왔다. 실행된 런에서 관측된 것이 아니라 **다음 라운드에서 반드시 관측될** 것들이다:

| # | 무엇이 틀렸나 | 어디서 | 고침 |
|---|---|---|---|
| 1 | **주차가 영원히 풀리지 않는다.** KTB-23의 해제는 merge 단계 (9)에 있는데, 하네스 PR은 구성상 보호 경로를 건드려 단계 (3)에서 자동 머지가 거부되고 **사람이 머지한다** — 단계 (9)는 정상 경로에서 실행되지 않는다. #2는 #24(하네스 이슈)가 머지돼도 `needs-info`에 그대로 앉는다 | `lib/merge-stage.js` (9) | sweeper에 `needs-info` 해제 팔 추가(quick+cron): 마지막 전이 사유가 `waiting for harness issue #m`이고 그 이슈가 닫혔으면 `needs-info → queue`. 단계 (9)는 빠른 경로로 유지 |
| 2 | **하네스 이슈가 하네스 이슈를 연다.** #15 같은 승격 이슈의 builder는 열려 있는 파일(`package.json`·`harness.toml`)을 프롬프트에서 여전히 "보호 경로"로 읽고 `harness_needed`를 채운 뒤 멈춘다 — L1이 그것으로 또 이슈를 연다(사슬) | `run-stage.js:431`, `workflows/factory-implement.js:245-288` | `&& !harnessIssue` 가드 + 프롬프트 변형(디스패처 `$2` → `harness_issue`): 규칙 8이 "이 이슈가 그 요청이다 — 고쳐라"로 바뀐다 + spec-conformance Lens 7 카브아웃 |
| 3 | **정리 스텝이 남의 락을 지운다.** `abortStage`는 claim에 실패해 물러난 런에서도 돌고, 그때 `release()`는 지금 돌고 있는 다른 러너의 `lock-15`를 지운다 | `run-stage.js:529`, `lib/claim.js` | `lockHolder()`로 소유자 확인 — 없으면 `already released`, 우리 것이면 삭제, 남의 것이면 `held by <other> — left alone`. 정리 스텝 조건도 `always() && job.status != 'success'`로 |
| 4 | **review가 blocked에서 못 돌아온다.** KTB-24가 세운 `awaiting-review → blocked`이 재시도 표에 없어, #15 같은 타임아웃은 **항상** 곧장 needs-human이었다(원인은 판정이 아니라 시간인데도) | `lib/labels.js`, `lib/sweeper.js` | `BLOCKED_RETRY.review` + `ENTRY_LABELS.review` + sweeper 행. hop은 `prerequisite: true`(복구이지 새 성취의 주장이 아니다) |

여기에 잔손질 넷: 하네스 이슈 dedupe를 제목 → 본문 마커(`factory-harness-request for=<n>`),
하네스 변형에서 `.factory/package-lock.json` 재차단, `sweep-step-last` 린트가 `- uses:` 스텝도 셈,
quick sweep의 API 호출량을 ADR에 명시.

### 라운드 7 — 고아 락이 네 번의 재점화를 삼켰다, 그리고 K가 물지 않았다 (KTB-28·29)

KTB-27(디스패처 `$ARGUMENTS`)이 `d823d5a`로 들어간 뒤의 관측. 두 결함 모두 **라이브**로 잡혔다.

| 이슈 | 무엇이 관측됐나 | 증거 | 판결 |
|---|---|---|---|
| **#15** | review 락 `refs/heads/factory/lock-15`가 **04:39에 타임아웃으로 사라진 런**(러너 `gha-34736609544`, KTB-24 정리 스텝 배포 **이전**)의 것으로 살아남았다. 그 뒤의 dispatch 5건(sweeper stalled 팔 ×4 + 수동)이 전부 26~40초 만에 `claim()`에서 죽었다 — **exit 0, run 기록 없음, 이슈 코멘트 없음, 잡 결론 `success`** | 잡 로그의 `already claimed by lock … runner=gha-34736609544`가 유일한 흔적. Actions 목록·이슈 타임라인 어디에서도 "아무 일도 하지 않았다"가 보이지 않았다 | **KTB-28**: (a) 소유자의 워크플로 런이 `completed`면 락은 잔해 — 지우고 다시 잡는다(조회 실패·로컬 러너는 살아 있는 것으로, fail closed). (b) 거부는 기록 + 코멘트 + `exit 2`. (c) sweeper의 두 dispatch 팔이 밀기 전에 잔해 락을 회수한다. (d) 같은 이슈+스테이지 재점화는 2회까지, 그 뒤 needs-human |
| **#18** | K=3인데 **리뷰 라운드 4**에 도달했고, 그 라운드도 `rework`으로 갔다 | `nextState`의 review 분기가 `decision === "approved"` 하나만 본다. `transition.js`의 `round ≤ K` 검사는 `factory:approved` 경로에만 걸리는데, K를 넘기는 것은 언제나 통과하지 **못하는** 쪽이다 — 발화하지 않는 자리에 있었다 | **KTB-29**: 판정이 approved가 아니고 `round >= K`면 `awaiting-review → needs-human`(사유 `review rounds exhausted (K=3): <n> must_fix remain`). approve는 어느 라운드에서든 통과한다 |
| #18 (관측 O21) | 같은 역할이 라운드 사이에 판정을 뒤집는 것이 두 번 관측됐다(approve → reject) | 라운드별 review handoff의 역할별 verdict 대조 | §12.4 지표로 승격: run 기록에 `review flips: <role> <before>→<after>` 한 줄(KTB-29에 포함) |
| — (KTB-22 검증) | API 쿼터(429)가 `factory:blocked` + 프로바이더 메시지 원문 + origin 마커로 떨어지는 것을 라이브로 확인 | 라운드 4의 판결(ADR-020 KTB-22)이 의도대로 동작 | 변경 없음 |

- **관측자 개입**: `lock-15`를 손으로 풀고(`claim.js`의 `release`) review를 다시 dispatch했다. 그 개입이
  필요했다는 사실 자체가 KTB-28 (a)·(c)의 근거다 — 공장은 자기 잔해를 스스로 치울 수 있어야 한다.
- **KTB-28 (b)의 대가**: 정상적인 claim 경쟁(라벨 이벤트와 sweeper dispatch가 같은 스테이지를 동시에
  물었을 때)도 이제 잡 실패로 기록된다. 실패한 잡 하나와 침묵 중에 전자를 골랐다.

### 라운드 8 — 라벨 스왑이 중간에 끊겨 이슈가 사라졌다 (KTB-30, 관찰 O20·O23)

KTB-28·29(`c361c7d`)를 배포하기 전, GitHub API가 5분간 불안정했던 창(08:50~08:55Z)에서 **같은 사고가 두 번**
났다. 둘 다 라이브다.

| 이슈 | 무엇이 관측됐나 | 증거 | 판결 |
|---|---|---|---|
| **#2** (08:52Z) | implement claim의 `planned → in-progress` 스왑이 중간에 실패해 **상태 라벨이 0개**로 남았다. implement는 exit 1, 이슈는 어떤 이벤트·sweeper 팔·`factory status`에도 나타나지 않았다 | `gh issue edit failed (1): … GraphQL: Something went wrong while executing your query`. 컨트롤러의 수동 `--add-label`도 한 번 실패(`failed to update 1 issue`)했고, **REST**(`gh api -X POST repos/…/issues/2/labels`)는 성공 → 그 라벨 변경이 `issues: labeled`를 쏴 implement가 다시 떴다 | **KTB-30**: (a) add-first 스왑(두 호출) (b) 1s·3s·9s 재시도 + REST 폴백 (c) 쓴 뒤 확인 1회(`label verify: repaired`) (d) 상태 라벨 0개 복구 팔 (e) 라벨 2개는 최신 전이의 `to`로 잇는다 (f) `factory status`의 `[no-state-label]` |
| **#15** (08:55Z) | review handoff의 `awaiting-review → approved` 스왑이 같은 방식으로 끊겼다 — **27분짜리(≈$10) 리뷰가 끝난 직후**에 그 결과가 라벨로 착지하지 못했다 | `gh issue edit failed (1): … EOF`. 컨트롤러가 REST로 `rework`을 손으로 세웠다(review R2: 1 approve / 3 reject) | 위와 같음 |
| — (관찰 O20) | #15가 sweeper stalled 팔에 **네 번** 밀렸다(05:14·05:45·07:42·08:25). 상한은 KTB-28 (d)로 이미 닫혔지만, 그 blocked들이 전부 "환경/크리덴셜"로 사람에게 넘어갔다 — 실제 원인은 잡 취소·타임아웃이었다 | 재점화 마커 4개 + 에스컬레이션 사유 문구 | origin 마커에 `cause=` 등급 추가: 취소는 R 예산을 쓰지 않고 취소마다 한 번 재시도, 에스컬레이션 사유가 원인을 이름으로 말한다(KTB-30의 O20 항목) |
| — (관찰 O23) | 08:50~08:55Z에 GraphQL 라벨 실패 2건 + `gh workflow run` HTTP 500이 겹쳤다. 같은 창에서 REST는 정상 | 위 두 행의 증거 + dispatch 500 로그 | 재시도만으로는 부족하다 — **다른 경로**(REST)가 폴백이어야 한다는 KTB-30 (b)의 근거 |

- **#2 plan 비용 $22.71 / 67분** — 옛 60분 잡 타임아웃이었으면 잘렸을 런이다(한도는 이미 90분).
- **관측자 개입**: 두 이슈 모두 REST로 라벨을 손으로 되살렸다(#2 → `planned` 뒤 재디스패치, #15 → `rework`).
  그 개입이 필요했다는 사실 자체가 KTB-30 (d)·(f)의 근거다 — 공장은 자기가 흘린 이슈를 스스로 주울 수 있어야 한다.
- **아직 도달 못 한 프로브**: 보호 경로 머지 거부(#15), KTB-25 라운드(#18), pg implement(#2).

## PRs/issues index

데모 저장소(`LeeHyeonKyu/know-thy-build-demo`)에서 이 dogfood 동안 열린 PR·이슈 전부를 종류별로 정리한다(각각의 상세는 위 Task 섹션과 `progress.md` 참조).

| # | 종류 | 무엇인가 |
|---|---|---|
| PR #1 | 업그레이드 | 첫 `chore(factory): init --upgrade` — KTB-2/3/4를 실어 옴, KTB-5(보호 경로는 L1로) 재적용 뒤 사람 머지로 통과한 첫 사례 |
| PR #3 | 업그레이드 | KTB-6(`additive_only`도 L1로) + KTB-7(스테이지 산출물 추출) 반영 |
| PR #4 | 업그레이드 | KTB-8/9(워크플로별 concurrency·tier 라벨) + lessons 삭제 정책 + KTB-7 리뷰 수정 배치 |
| PR #9 | 업그레이드 | KTB-11(`files.stale` 병합 판정) + G1(`[runtime].setup` 문서화) |
| **PR #10** | **첫 머지된 factory PR** | 이슈 #8(healthz 캐시 가능성 버그) 구현 — `claude/fq-8`, triage→plan→implement→review 무개입 통과, merge 2회 재시도(KTB-15 draft, KTB-19 큐잉 체크) 끝에 머지 → **첫 다크 완주(2026-09-12 17:07Z)** |
| PR #11 | 업그레이드 | KTB-13(allow 목록을 도구 부여로) |
| PR #12 | 업그레이드 | KTB-13 r1(훅이 실제로 막는 것 확장) |
| PR #13 | 업그레이드 | KTB-14/14 r1/15/15b 일부/16/17 + KTB-13 r2 배치 |
| PR #16 | 업그레이드 | KTB-18/19 + KTB-15b 나머지 배치 |
| **이슈 #2** | **standard tier → KTB-23** | "001 Create note" — 다크 처리 시도 4회(plan 실패×2 KTB-7/16, implement 빈 diff RED KTB-13, 라운드 6 implement verifier reject) 끝에 `factory:needs-human`으로 남음. 라운드 6에서 원인이 드러났다: dw1/dw3/dw4가 `pg` 패키지를 요구하는데 `package.json`이 보호 경로라 builder가 못 건드리고, 프롬프트가 시킨 대응은 PR 본문에 "Harness change needed"라고 **산문으로** 쓰는 것이었다 — 그 산문을 읽는 기계가 없어 gates GREEN 4/4에도 verifier가 거부하고 needs-human으로 갔다. 사람이 재큐하면 같은 벽. 네 라운드 ≈$67, 머지 0건. 판결: 요청을 implement handoff의 `harness_needed[{file,change,why}]` 필드로 만들고, L1이 `factory:harness` 이슈 하나를 열고 피처를 `needs-info`로 주차했다가 **그 하네스 이슈가 닫히면 sweeper가** `queue`로 되돌린다(ADR-020 KTB-23 + r1 — 하네스 PR은 사람이 머지하므로 merge 스테이지의 해제 코드는 정상 경로에서 돌지 않는다). 당시 수동 우회는 PR #24(사람이 `pg`를 추가해 머지) |
| 이슈 #5 | standard tier | "002 list notes" — Task 4 백로그(002는 001에 depends_on, #2 머지 후 큐잉 예정) |
| 이슈 #6 | standard tier | "003 search" — Task 4 백로그 |
| 이슈 #7 | flaky tier | "004 cache expiry" — 의도적으로 시간 의존 테스트를 심어 §5.2.5 flaky 경로를 관찰할 목적의 백로그 |
| **이슈 #8** | standard tier | "healthz response is cacheable" 버그(`:issue` 형식, 스펙 파일 없음) — PR #10으로 **첫 다크 완주** |
| 이슈 #14 | §12.4 skip-attempt probe | `backlog` 위에 `factory:approved`를 손으로 얹은 의도적 프로브 — KTB-18(라벨-셋 모호성) 발견, 손 라벨 건너뛰기 시도는 침묵 없이 차단됨(needs-human) 확인 |
| 이슈 #15 | harness 승격 | 첫 전체 retro(머지 1건 뒤)가 `http-at-m1` 성숙도 갭을 발견해 자동 생성한 `harness: promote to M2` — `factory:harness` 라벨, PR #20까지 진행(관찰 O15) |
| **PR #20** | **관찰 O15 → KTB-20** | 이슈 #15의 "승격 PR"인데 **승격이 없다**: builder가 `.factory/harness.toml`·`docker-compose.test.yml`·playwright 설정을 하나도 쓰지 못하고(ci-settings.json의 `Edit/Write(.factory/**)` + `block-dangerous.sh`) 전부 "사람이 해 주세요"로 미뤘다. 두 방벽은 설계대로 동작했다 — 설계가 이 이슈 종류를 구분하지 않았을 뿐이다. 판결: `factory:harness` 이슈의 implement만 `.factory/ci-settings-harness.json` + `FACTORY_HARNESS_ISSUE=1`로 돌려 **테스트 인프라 파일만** 열고, 머지는 그대로 사람(ADR-020 KTB-20) |
| 이슈 #18 | 관찰 O14 | README만 고치는 문서 이슈인데 tier가 `standard`로 판정됐다 — `:issue` 스킬이 늘 넣는 회귀 가드 `test_NNN_<slug>` 때문에 diff가 문서만이 아니게 됐고, CHARTER의 `docs` tier 조건("diff가 `docs/**`, `*.md`만")을 스스로 깨뜨렸다. 판결: Impact paths가 전부 문서면 가드 테스트 없이 `done_when`을 쓴다(ADR-020 O14) |
| **이슈 #18 review** | **KTB-21** | review 스테이지, 4/4 승인인데도 `factory:approved`로 전이 거부 — 사유 "gates file status is RED": `unit`이 `service "db" is not running`. 트랜스크립트: qa 리뷰어가 증거 수집 중 `docker compose -f docker-compose.test.yml down`을 실행(setup이 잡 시작 시 올려 둔 env를 스스로 내림), 게이트는 28분 뒤 죽은 env에 대고 돌아 RED. 판결: (1) `deny-all-writes.sh`(읽기 전용 역할)가 `docker compose/docker`의 `down/stop/rm/kill/restart`와 `up`을 전부 막고 `ps`/`logs`/`exec … psql` 같은 점검만 허용, `block-dangerous.sh`(builder)는 teardown 동사만 막고 `up`(멱등)은 그대로 둔다. (2) 방어 종심: `gates.js`가 `[commands]`를 돌리기 전에 `.factory/bin/test-env.js up`으로 한 번 더 re-up하고(멱등), 실패하면 RED가 아니라 BLOCKED(판정 불가)로 세운다 — 죽은 env로 낸 GREEN/RED는 애초에 신뢰할 수 없다(ADR-020 KTB-21) |
| **라운드 4, 2026-09-12 20:20Z** | **KTB-22** | API 쿼터 소진(429, 조직 월 지출 한도)이 세 스테이지를 동시에 죽였다 — #2 라운드 4 재실행 implement, #15(harness 승격) implement, #18 plan. 셋 다 1턴·299 ms 만에 같은 봉투(`is_error:true, api_error_status:429, terminal_reason:"api_error"`)로 끝났는데 `factory:needs-human`으로 올라갔다 — 사람이 볼 산출물도 고칠 프롬프트도 없는 순수 환경 조건인데 등급이 틀렸다. 판결: 등급은 `factory:blocked`, 사유는 프로바이더 메시지 원문, sweeper는 이 origin에 한해(사유에 "api error") 3번까지 재시도한 뒤 사람에게 넘긴다(ADR-020 KTB-22) |

